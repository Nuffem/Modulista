services:
  app:
    build:
      context: .
      dockerfile: Containerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - HOME=/app
      - CHOKIDAR_USEPOLLING=true
